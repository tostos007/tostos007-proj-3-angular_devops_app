---
- name: Create deployment directory
  file:
    path: "{{ deploy_path }}"
    state: directory
    mode: '0755'

- name: Create versions directory
  file:
    path: "{{ deploy_path }}/versions/{{ build_version }}"
    state: directory
    mode: '0755'

- name: Copy artifact to server
  delegate_to: localhost   # ✅ This runs the task on Jenkins (control node)
  copy:
    src: "{{ provided_artifact_name }}"
    dest: "{{ deploy_path }}/{{ build_version }}/"
    remote_src: yes  # Assuming artifact is on Jenkins server

- name: Extract artifact to versioned directory
  unarchive:
    src: "/tmp/{{ artifact_path }}"
    dest: "{{ deploy_path }}/versions/{{ current_version }}"
    remote_src: yes
    creates: "{{ deploy_path }}/versions/{{ current_version }}"

- name: Update symlink to current version
  file:
    src: "{{ deploy_path }}/versions/{{ current_version }}"
    dest: "{{ app_symlink }}"
    state: link
    force: yes

- name: Set permissions for web content
  file:
    path: "{{ deploy_path }}"
    state: directory
    recurse: yes
    owner: www-data
    group: www-data
    mode: '0755'
...